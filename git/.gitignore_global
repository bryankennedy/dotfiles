#
# This file is managed by Ansible. Do not edit directly. Your changes will be overwritten.
#

#
# Global Git Ignores
#

# Compiled source
*.com
*.class
*.dll
*.exe
*.o
*.so

# Packages
#
# It's better to unpack these files and commit the raw source
# Git has its own built in compression methods
*.7z
*.dmg
*.iso
*.jar
*.rar
*.tar
*.zip

# Logs and databases
*.log
# *.sql
*.sqlite

# OS X generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Ingoring JetBrains IDE project files
.idea
.ropeproject

# Python compiled files
.pyc

# Fabric autocomplete caches
.fab_tasks~

# npm package-lock.json files
# We are standardizing on using yarn, which creates its own
# lock file. We should use the yarn.lock instead of package-lock.json
# Ignoring this ensure that it won't get added to new projects.
package-lock.json

.vscode
